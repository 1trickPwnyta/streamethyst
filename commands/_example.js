/**
	command.context: Twitch chat context
	command.parameters: Command parameters
	command.message: Entire message after the command without parsing parameters
	command.respond(identity, message):
		identity: label used to identify the identity to respond as, for example "streamer"
		message: message to respond with
*/

module.exports = command => {
	
	// Code here will execute when, for instance, !_example is entered in chat
	
};
